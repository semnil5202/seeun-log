---
/** 제품 정보 카드. Schema.org Product 마크업을 포함한다. */

import type { Locale } from "@/shared/types/common";
import { t } from "@/shared/lib/i18n/translations";
import { getLocalePath } from "@/shared/lib/i18n/locales";

interface Props {
  category: string;
  subCategory: string;
  categoryLabel: string;
  subCategoryLabel: string;
  productNames: string[] | null;
  translatedProductNames?: string[] | null;
  purchaseSources: string[] | null;
  translatedPurchaseSources?: string[] | null;
  purchaseLinks: string[] | null;
  prices?: number[] | null;
  pricePrefixes?: string[] | null;
  translatedPricePrefixes?: string[] | null;
  description: string;
  locale: Locale;
}

const {
  category,
  subCategory,
  categoryLabel,
  subCategoryLabel,
  productNames,
  translatedProductNames,
  purchaseSources,
  translatedPurchaseSources,
  purchaseLinks,
  prices,
  pricePrefixes,
  translatedPricePrefixes,
  description,
  locale,
} = Astro.props;

const summaryLabel = t("post.summary", locale);
const summaryLines = description ? description.split('\n').filter(Boolean) : [];
const targetCurrency = t("place.targetCurrency", locale);

const displayProductNames = translatedProductNames ?? productNames;
const displayPurchaseSources = translatedPurchaseSources ?? purchaseSources;
const displayPricePrefixes = translatedPricePrefixes ?? pricePrefixes;

const products = (displayProductNames ?? []).map((name, i) => ({
  name,
  source: displayPurchaseSources?.[i] ?? '',
  link: purchaseLinks?.[i] ?? '',
  pricePrefix: displayPricePrefixes?.[i] ?? '',
  price: prices?.[i] ?? null,
}));
---

<div
  class="bg-gray-50 border border-gray-200 p-5 mb-6"
  itemscope
  itemtype="https://schema.org/Product"
>
  <dl class="space-y-3 text-sm">
    <div class="flex items-baseline gap-3">
      <dt class="shrink-0 w-20 text-gray-500 font-medium">{t("place.category", locale)}</dt>
      <dd class="text-gray-900">
        <a href={getLocalePath(`/${category}/`, locale)} class="text-primary-600 underline underline-offset-4 hover:text-primary-700 transition-colors">{categoryLabel}</a>
        <span class="text-base leading-none">›</span>
        <a href={getLocalePath(`/${category}/${subCategory}/`, locale)} class="text-primary-600 underline underline-offset-4 hover:text-primary-700 transition-colors">{subCategoryLabel}</a>
      </dd>
    </div>
    <div class:list={[
      products.length > 1 && 'grid grid-cols-1 lg:grid-cols-2 gap-3 border-t border-b border-gray-200 py-3',
    ]}>
      {products.map((product) => (
        <div class="space-y-1.5">
          <div class="flex items-baseline gap-3">
            <dt class="shrink-0 w-16 text-gray-500 font-medium">{t("product.name", locale)}</dt>
            <dd class="text-gray-900 font-semibold" itemprop="name">
              {product.name}
            </dd>
          </div>
          {product.source && (
            <div class="flex items-baseline gap-3">
              <dt class="shrink-0 w-16 text-gray-500 font-medium">{t("product.source", locale)}</dt>
              <dd class="flex items-baseline gap-1.5">
                <span class="text-gray-900">{product.source}</span>
                {product.link && (
                  <a
                    href={product.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="shrink-0 translate-y-px text-gray-400 hover:text-gray-600 transition-colors"
                    aria-label={product.source}
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <title>{product.source}</title>
                      <desc>{product.source}</desc>
                      <path d="M7 7h10v10"></path>
                      <path d="M7 17 17 7"></path>
                    </svg>
                  </a>
                )}
              </dd>
            </div>
          )}
          {product.price != null && (
            <div class="flex items-baseline gap-3">
              <dt class="shrink-0 w-16 text-gray-500 font-medium">{t("place.price", locale)}</dt>
              <dd class="flex items-baseline gap-1.5">
                <span class="text-gray-900">{product.pricePrefix}{product.price.toLocaleString()}</span>
                {targetCurrency && (
                  <a
                    href={`https://www.google.com/search?q=${product.price}+KRW+to+${targetCurrency}`}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="shrink-0 translate-y-px text-gray-400 hover:text-gray-600 transition-colors"
                    aria-label={t("a11y.currencyConverter", locale)}
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <title>{t("a11y.currencyConverter", locale)}</title>
                      <desc>{t("a11y.currencyConverter", locale)}</desc>
                      <path d="M7 7h10v10"></path>
                      <path d="M7 17 17 7"></path>
                    </svg>
                  </a>
                )}
              </dd>
            </div>
          )}
        </div>
      ))}
    </div>
    {summaryLines.length > 0 && (
      <div class="flex items-baseline gap-3">
        <dt class="shrink-0 w-20 text-gray-500 font-medium">{summaryLabel}</dt>
        <dd class="text-gray-900">
          <ul class="space-y-0.5">
            {summaryLines.map((line) => (
              <li class="pl-3 -indent-3">⋅ {line}</li>
            ))}
          </ul>
        </dd>
      </div>
    )}
  </dl>
</div>
