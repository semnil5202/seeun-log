---
/** 추천 뱃지. */

interface Props {
  label: string;
  class?: string;
}

const { label, class: className = "" } = Astro.props;
---

<span class:list={[
  "inline-flex items-center text-xs font-semibold bg-primary-600 text-white rounded-md px-2.5 py-0.5",
  className,
]}>
  {label}
</span>
